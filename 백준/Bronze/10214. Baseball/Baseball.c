#include <stdio.h>

int main() {

	int T;
	int Y, K;
	int Ys = 0, Ks = 0;
	scanf("%d", &T);

	while (T--) {
		for (int i = 0; i < 9; i++) {
			scanf("%d %d", &Y, &K);
		}
		printf("Yonsei\n");
	}
	
}